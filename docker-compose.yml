version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  judge0:
    image: judge0/judge0:latest
    ports:
      - "2358:2358"
    environment:
      REDIS_URL: redis://redis:6379
    privileged: true
